const defaultInput = `<div class="input-wrap">
  <span class="label">{{label}}</span>
  <input {{inputProps}} name="{{name}}" class="input" type="{{type}}"/>
  <span class="error">{{errorText}}</span>
</div>`;

export default defaultInput;
